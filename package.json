{
  "name": "lnl-lambda-demo",
  "version": "1.0.0",
  "description": "Lambda demo for SolDevelo L&L",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests exists, sorry\" && exit 1",
    "deploy": " — zip-file fileb: //Lambda-Deployment.zip",
    "predeploy": "zip -r Lambda-Deployment.zip * -x *.zip *.log"
  },
  "author": "Pawel Gesek",
  "license": "MIT",
  "dependencies": {
    "pg": "^6.1.2"
  }
}
